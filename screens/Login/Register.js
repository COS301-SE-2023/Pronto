import {
  StyleSheet,
  View,
  Text,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import React from "react";

const Register = ({ navigation }) => {
  return (
    <SafeAreaView>
      <Text>Register Page</Text>
    </SafeAreaView>
  );
};

export default Register;
