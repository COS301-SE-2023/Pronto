{
  "name": "pronto-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "test:watch": "jest  --watch",
    "test:watch:silent": "jest --watch --silent",
    "test:unit": "jest unit",
    "test:unit:silent": "jest unit --silent",
    "test:unit:watch": "jest --watch unit",
    "test:unit:watch:silent": "jest --watch --silent unit"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.363.0",
    "@aws-sdk/lib-dynamodb": "^3.428.0",
    "jest": "^29.6.0"
  }
}
